# VIPER Trading Bot - USDT Swap Configuration
# Complete configuration for Bitget USDT-M futures trading

## API Configuration
BITGET_API_KEY=your_api_key_here
BITGET_API_SECRET=your_api_secret_here
BITGET_API_PASSWORD=your_passphrase_here

## USDT Swap Trading Configuration
DEFAULT_LEVERAGE=20
MAX_LEVERAGE=125
RISK_PER_TRADE=0.02
POSITION_SIZE_USDT=100
MAX_CONCURRENT_POSITIONS=10

## USDT Swap Symbols (Priority List)
# Major cryptocurrencies in USDT-M futures format
USDT_SWAP_SYMBOLS=BTCUSDT_UMCBL,ETHUSDT_UMCBL,BNBUSDT_UMCBL,ADAUSDT_UMCBL,SOLUSDT_UMCBL,MATICUSDT_UMCBL,AVAXUSDT_UMCBL,DOTUSDT_UMCBL,LINKUSDT_UMCBL

## VIPER Scoring Configuration for USDT Swaps
VIPER_THRESHOLD_HIGH=75
VIPER_THRESHOLD_MEDIUM=60
EXECUTION_COST_THRESHOLD=3.0

# Execution cost optimization for USDT swaps
# Lower costs due to futures market liquidity
BTC_BASE_SPREAD_COST=0.12
ETH_BASE_SPREAD_COST=0.16
MAJOR_ALT_BASE_SPREAD_COST=0.25
OTHER_BASE_SPREAD_COST=0.50

## Service Configuration
EXCHANGE_CONNECTOR_PORT=8000
VIPER_SCORING_SERVICE_PORT=8009
MARKET_DATA_MANAGER_PORT=8001
LIVE_TRADING_ENGINE_PORT=8002

## Redis Configuration
REDIS_URL=redis://redis:6379

## Rate Limiting
RATE_LIMIT_REQUESTS_PER_SECOND=2.0
RATE_LIMIT_BUFFER=0.1

## Market Data Configuration
VOLUME_PERIOD=24
PRICE_PERIOD=4
RANGE_PERIOD=24
CACHE_TTL=300

## Logging
LOG_LEVEL=INFO
DEBUG_MODE=false

## Network Configuration
# Bitget USDT swap API endpoints
BITGET_BASE_URL=https://api.bitget.com
BITGET_SWAP_ACCOUNT_ENDPOINT=/api/mix/v1/account/account
BITGET_SWAP_POSITIONS_ENDPOINT=/api/mix/v1/position/allPosition
BITGET_SWAP_TICKER_ENDPOINT=/api/mix/v1/market/ticker
BITGET_SWAP_ORDERBOOK_ENDPOINT=/api/mix/v1/market/depth
BITGET_SWAP_KLINES_ENDPOINT=/api/mix/v1/market/candles
BITGET_SWAP_PLACE_ORDER_ENDPOINT=/api/mix/v1/order/placeOrder
BITGET_SWAP_CANCEL_ORDER_ENDPOINT=/api/mix/v1/order/cancel-order
BITGET_SWAP_ORDER_STATUS_ENDPOINT=/api/mix/v1/order/detail
BITGET_SWAP_OPEN_ORDERS_ENDPOINT=/api/mix/v1/order/current
BITGET_SWAP_LEVERAGE_ENDPOINT=/api/mix/v1/account/setLeverage

## Security Configuration
# All API calls use HMAC-SHA256 authentication
# Signature format: timestamp + method + requestPath + body
# Headers required: ACCESS-KEY, ACCESS-SIGN, ACCESS-TIMESTAMP, ACCESS-PASSPHRASE

## Trading Configuration
# Product Type: UMCBL (USDT-M futures)
# Margin Coin: USDT
# Order Types: market, limit
# Time in Force: IOC (market), GTC (limit)
# Sides: open_long, open_short, close_long, close_short

## Volume Thresholds for Cost Calculation
VERY_HIGH_VOLUME_THRESHOLD=50000000  # $50M+ (30% discount)
HIGH_VOLUME_THRESHOLD=20000000       # $20M+ (10% discount)
GOOD_VOLUME_THRESHOLD=10000000       # $10M+ (normal cost)
MODERATE_VOLUME_THRESHOLD=5000000    # $5M+ (30% increase)
LOW_VOLUME_THRESHOLD=1000000         # $1M+ (2x cost)
# Below $1M: 5x cost (should be avoided)

## Signal Configuration
SIGNAL_COOLDOWN=300  # 5 minutes between signals per symbol
MAX_SIGNALS_PER_SYMBOL=3
SIGNAL_EXPIRY=1800   # 30 minutes

## Circuit Breaker Configuration
MAX_DAILY_LOSSES=1000  # USDT
MAX_CONSECUTIVE_LOSSES=5
CIRCUIT_BREAKER_COOLDOWN=3600  # 1 hour